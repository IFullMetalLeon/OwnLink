<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="История звонков"            
             x:Class="OwnLink.View.HistoryCallPage">
    <ContentPage.Content>
        <StackLayout BackgroundColor="White">
            <ListView ItemsSource="{Binding historyCalls}"
                      HasUnevenRows="True"
                      SeparatorVisibility="None"
                      VerticalScrollBarVisibility="Default">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid Padding="0" Margin="15,0,1,0" BackgroundColor="White">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="1*"/>
                                    <RowDefinition Height="1*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Row="0" Grid.Column="0" Text="{Binding Caller}" TextColor="#000000" FontSize="16" HorizontalTextAlignment="Start"/>
                                <Label Grid.Row="0" Grid.Column="1" Text="{Binding DateShow}" TextColor="#9E9E9E" FontSize="14" HorizontalTextAlignment="End"/>
                                <Grid Grid.Row="1" Grid.Column="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="8*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackLayout Grid.Column="0">
                                        <Image Source="{Binding IconName}" HeightRequest="12" WidthRequest="12" Aspect="AspectFit"/>
                                    </StackLayout>
                                    <Label Grid.Column="1" Text="{Binding Duration}" TextColor="{Binding DurationTC}" FontSize="12"/>    
                                </Grid>
                                                               
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>